@model IEnumerable<WebOnlineShopping.Models.DeliveryStatus>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Decription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Decription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeliveryStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeliveryStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeliveryStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
